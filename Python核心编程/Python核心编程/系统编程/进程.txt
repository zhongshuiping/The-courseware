进程：正在运行的程序叫做进程  ps aux，拥有内存和cpu的资源
程序： xx.py的文件，是静态的，没有执行的一段代码 
进程三个状态：就绪（ready）  运行（run）  等待（block）
主进程：默认存在的
子进程：新创建出来的
一个进程对应一个进程pid
多进程中，每个进程数据（包括全局变量）都各自用户一份，不影响
多个进程的执行顺序是无序的
如何查看当前进程的父进程的pid，os.getppid() 
terminate在调用的时候，操作系统终止进程会有一定的时间，等待子进程退出后在判断状态